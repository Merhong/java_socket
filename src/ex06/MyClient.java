package ex06;import java.io.BufferedReader;import java.io.BufferedWriter;import java.io.IOException;import java.io.InputStreamReader;import java.io.OutputStreamWriter;import java.net.Socket;import java.util.Scanner;/* 양방향 통신(클라이언트) */public class MyClient {		public MyClient() throws IOException {		// 1. 서버 연결 (localhost == 127.0.0.1) ==> IP주소 : 192.168.200.115		// 10, 172, 192로 시작하는 IP는 사설 IP임		Socket socket = new Socket("192.168.200.58", 10000); // 루프백 주소(자기자신)				// 2. 버퍼 생성		BufferedReader reader = new BufferedReader(			new InputStreamReader(socket.getInputStream(), "utf-8"));		BufferedWriter writer = new BufferedWriter(			new OutputStreamWriter(socket.getOutputStream(), "utf-8"));		Scanner keyboard = new Scanner(System.in, "utf-8");				// 쓰기 스레드		new Thread(() -> {			try {				while (true) {					String msg = keyboard.nextLine();					writer.write("User1:" + msg);					writer.write("\n");					writer.flush();				}			} catch (Exception e) {				e.printStackTrace();			}		}).start();				// 읽기 스레드		new Thread(() -> {			try {				while (true) {					String msg = reader.readLine(); // 버퍼 비우기					System.out.println("서버로부터 받은 메세지 : " + msg);				}			} catch (Exception e) {				e.printStackTrace();			}		}).start();	}		public static void main(String[] args) {		try {			new MyClient();		} catch (Exception e) {			e.printStackTrace();		}	}}